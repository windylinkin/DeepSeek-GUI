# Private Repo Setup

Current GitHub repository:

`windylinkin/DeepSeek-GUI`

Current private development branch:

`private/agentos`

Recommended local clone folder:

`D:/ai/deepseek-gui-agentos-private`

Recommended remotes:

- origin: the user's private DeepSeek-GUI repository
- upstream: the official DeepSeek-GUI repository

Suggested local setup:

```bash
git clone https://github.com/windylinkin/DeepSeek-GUI.git deepseek-gui-agentos-private
cd deepseek-gui-agentos-private
git checkout private/agentos
```

If the repository is later moved to a private remote, keep this branch name and keep upstream sync notes in this folder.
